{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capability set for the bundled CodeXRay window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
